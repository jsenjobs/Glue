package app.chaosstudio.com.glue.greendb.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import app.chaosstudio.com.glue.greendb.model.WhiteDomain;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "WHITE_DOMAIN".
*/
public class WhiteDomainDao extends AbstractDao<WhiteDomain, Long> {

    public static final String TABLENAME = "WHITE_DOMAIN";

    /**
     * Properties of entity WhiteDomain.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Domain = new Property(1, String.class, "domain", false, "DOMAIN");
        public final static Property Tag = new Property(2, String.class, "tag", false, "TAG");
    }


    public WhiteDomainDao(DaoConfig config) {
        super(config);
    }
    
    public WhiteDomainDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"WHITE_DOMAIN\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"DOMAIN\" TEXT," + // 1: domain
                "\"TAG\" TEXT);"); // 2: tag
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"WHITE_DOMAIN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, WhiteDomain entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String domain = entity.getDomain();
        if (domain != null) {
            stmt.bindString(2, domain);
        }
 
        String tag = entity.getTag();
        if (tag != null) {
            stmt.bindString(3, tag);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, WhiteDomain entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String domain = entity.getDomain();
        if (domain != null) {
            stmt.bindString(2, domain);
        }
 
        String tag = entity.getTag();
        if (tag != null) {
            stmt.bindString(3, tag);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public WhiteDomain readEntity(Cursor cursor, int offset) {
        WhiteDomain entity = new WhiteDomain( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // domain
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2) // tag
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, WhiteDomain entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setDomain(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setTag(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(WhiteDomain entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(WhiteDomain entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(WhiteDomain entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
