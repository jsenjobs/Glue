package app.chaosstudio.com.glue.greendb.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import app.chaosstudio.com.glue.greendb.model.OpendedUrl;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "OPENDED_URL".
*/
public class OpendedUrlDao extends AbstractDao<OpendedUrl, Long> {

    public static final String TABLENAME = "OPENDED_URL";

    /**
     * Properties of entity OpendedUrl.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Domain = new Property(1, String.class, "domain", false, "DOMAIN");
        public final static Property Uuid = new Property(2, String.class, "uuid", false, "UUID");
    }


    public OpendedUrlDao(DaoConfig config) {
        super(config);
    }
    
    public OpendedUrlDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"OPENDED_URL\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"DOMAIN\" TEXT," + // 1: domain
                "\"UUID\" TEXT);"); // 2: uuid
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"OPENDED_URL\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, OpendedUrl entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String domain = entity.getDomain();
        if (domain != null) {
            stmt.bindString(2, domain);
        }
 
        String uuid = entity.getUuid();
        if (uuid != null) {
            stmt.bindString(3, uuid);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, OpendedUrl entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String domain = entity.getDomain();
        if (domain != null) {
            stmt.bindString(2, domain);
        }
 
        String uuid = entity.getUuid();
        if (uuid != null) {
            stmt.bindString(3, uuid);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public OpendedUrl readEntity(Cursor cursor, int offset) {
        OpendedUrl entity = new OpendedUrl( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // domain
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2) // uuid
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, OpendedUrl entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setDomain(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setUuid(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(OpendedUrl entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(OpendedUrl entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(OpendedUrl entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
